// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

/**
 * @dev 共识金库只读接口（占位，供 PredictionMarketFactory 编译通过）
 */
interface IConsensusVaultView {
    // 占位，实际校验在 PredictionMarketFactory 内用 IConsensusVaultFactory
}
